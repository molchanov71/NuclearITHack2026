# Документация фундамента

В `Doxyfile` включена русская генерация doxygen по `main.cpp` и каталогу `src`.

Быстрый запуск:

```bash
doxygen docs/Doxyfile
```

После этого HTML-вывод будет лежать в `docs/build/html`.
