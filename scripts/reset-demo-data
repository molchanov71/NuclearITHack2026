#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
DATA_DIR="${XDG_DATA_HOME:-${HOME}/.local/share}/NuclearITHack/NuclearITHack2026"

rm -rf "${DATA_DIR}"
echo "Удалён каталог данных: ${DATA_DIR}"
